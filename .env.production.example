# Production Environment Configuration Example
# Copy this file to .env.production and update with your production values

# Base URL for Production Application
BASE_URL=https://your-production-app.com

# MongoDB Production Database
# Use a secure connection string with authentication
MONGODB_URI=mongodb://username:password@production-mongodb-host:27017/production_database?authSource=admin

# Optional: Use MongoDB Atlas (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Test Timeout (milliseconds)
# Consider increasing for production environments with slower networks
TEST_TIMEOUT=60000

# Environment
NODE_ENV=production
